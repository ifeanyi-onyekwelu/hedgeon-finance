<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Verify Your Email</title>
    <style>
        body {
            background: #f5f7fa;
            font-family: 'Segoe UI', sans-serif;
            padding: 20px;
        }

        .box {
            max-width: 600px;
            background: #ffffff;
            padding: 30px;
            margin: auto;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.08);
            text-align: center;
        }

        .header {
            font-size: 24px;
            color: #4CAF50;
        }

        .code-box {
            margin: 20px auto;
            background: #eee;
            padding: 14px 28px;
            font-size: 20px;
            font-weight: bold;
            color: #333;
            letter-spacing: 4px;
            border-radius: 8px;
            display: inline-block;
        }

        .footer {
            margin-top: 40px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="box">
        <h2 class="header">Verify Your Email</h2>
        <p>Hi <strong>
                <%= userName %>
            </strong>,</p>
        <p>You're almost ready to start using Hedgeon Finance Capital. Please verify your email using the code below:
        </p>
        <div class="code-box">
            <%= verificationCode %>
        </div>
        <p>This code will expire shortly, so use it soon.</p>
        <div class="footer">
            &copy; <%= new Date().getFullYear() %> Hedgeon Finance Capital. All rights reserved.
        </div>
    </div>
</body>

</html>