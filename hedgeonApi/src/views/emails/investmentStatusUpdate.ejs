<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Investment Status Update</title>
    <style>
        body {
            font-family: sans-serif;
            line-height: 1.6;
            color: #333;
        }

        .container {
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            max-width: 600px;
            margin: 20px auto;
        }

        .header {
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 15px;
        }

        .status {
            padding: 10px;
            background-color: #f4f4f4;
            border-left: 4px solid #2196F3;
            margin: 15px 0;
        }

        .footer {
            margin-top: 20px;
            font-size: 0.9em;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="container">
        <p class="header">Hello <%= userName %>,</p>

        <p>
            <%= message %>
        </p>

        <div class="status">
            Current Status: <strong>
                <%= status.toUpperCase() %>
            </strong>
        </div>

        <% if (status==='completed' ) { %>
            <p>Congratulations! Your investment has been successfully completed. You can now review your returns in your
                portfolio.</p>
            <% } else if (status==='paused' ) { %>
                <p>Your investment is temporarily paused. Please contact support if you have any questions.</p>
                <% } else if (status==='cancelled' ) { %>
                    <p>We're sorry to inform you that your investment was cancelled. Reach out if you need further
                        clarification.</p>
                    <% } else if (status==='active' ) { %>
                        <p>Your investment is now active and accumulating returns.</p>
                        <% } %>

                            <p class="footer">
                                Thank you,<br>
                                The Hedgeon Finance Team
                            </p>
    </div>
</body>

</html>