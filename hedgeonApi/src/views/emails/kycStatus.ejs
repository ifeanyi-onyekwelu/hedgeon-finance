<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>KYC Status Update</title>
    <style>
        /* Add some basic CSS for styling your email */
        body {
            font-family: sans-serif;
            line-height: 1.6;
            color: #333;
        }

        .container {
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            max-width: 600px;
            margin: 20px auto;
        }

        .header {
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 15px;
        }

        .footer {
            margin-top: 20px;
            font-size: 0.9em;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="container">
        <p class="header">Hello <%= userName %>,</p>

        <p>
            <%= message %>
        </p>
        <% if (status==='approved' ) { %>
            <p>Your account is now fully verified. You can explore all features.</p>
            <% } else if (status==='rejected' ) { %>
                <p>Please review the feedback provided in your account and resubmit if necessary.</p>

                <% } %>

                    <p class="footer">
                        Thank you,<br>
                        The Hedgeon Finance Team
                    </p>
    </div>
</body>

</html>